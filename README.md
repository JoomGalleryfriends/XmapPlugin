# XmapPlugin
With the plugin you can add entries of categories and images from JoomGallery to the sitemap generated by Xmap.

Install the plugin with the extension manager of Joomla! and configure it via Xmap -> sitemaps Manager -> Extensions.
